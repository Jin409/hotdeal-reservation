# DECISIONS.md

설계 과정에서 고민했던 기술적 판단과 트레이드오프를 기록합니다.

---

## 쟁점 1. 재고 정합성 - 동시성 제어 방식 선택

### 상황

분산 환경(서버 2대)에서 10개 한정 재고를 정확히 차감해야 함. 00시 기준 500~1000 TPS 급증 예상.

### 선택지

- DB 비관적 락 (SELECT FOR UPDATE)
- Redisson 분산 락
- Redis 원자적 감소 연산 (DECR)
- Redis Lua 스크립트
- Redis 재고 Hold

### Redisson 분산 락 기각

분산 환경에서 락을 보장하지만 한 번에 하나씩 직렬 처리.
고TPS 환경에서 락 대기가 폭발하고, 대기열로 이미 순차 처리를 보장하므로 중복.

### Redis DECR 검토 및 기각

락 없이 원자적으로 차감하며 음수 시 즉시 원복하는 방식.
초기 설계에서 채택했으나 아래 이유로 기각:

```
- 앱 시작 시 DB → Redis 재고 동기화 필요
- 5초 주기 헬스체크 + 복구 시 재동기화 필요
- 매 차감마다 DB도 동기화 + FOR UPDATE 락 필요
- 실패 시 Redis INCR 롤백 필요
→ 복잡도 대비 실익이 없다고 판단
```

### Lua 스크립트 검토 및 기각

```
- 문자열로 관리되어 IDE 지원 없음 → 오타가 런타임에야 발견
- 단위 테스트 불가, 디버깅 어려움
```

### Redis 재고 Hold 검토 및 기각

결제 전 임시 선점 상태를 Redis TTL로 관리하는 방식.

고트래픽에서 DB 부하를 줄이는 장점이 있으나 기각:
- 대기열을 통해 순차적으로 결제 단계에 진입하도록 설계했기에 여러 사용자가 동시에 동일 재고를 선점하는 상황을 구조적으로 제거
- Redis 기반 임시 재고 hold는 추가적인 정합성 이점을 제공하지 않는다고 판단

```
대기열 없이 다수가 동시에 booking 진입하는 구조라면 → Redis Hold 유리
대기열로 1명씩 순차 진입하는 현재 구조에서는 → 과설계
```

**→ DB 비관적 락 채택. Redis는 재고 관리에서 제외.**

### Redis와 DB 역할 분리

```
Redis  → 대기열 순서 보장, 트래픽 완충, 사용자 진입 제어
MySQL  → 최종 재고 정합성의 기준 (Source of Truth)
```

Redis 장애 시 재고 데이터가 깨지는 문제가 아닌, 대기열 진입/진행이 제한되는 문제로 축소

### 트레이드오프

```
DB 비관적 락 단순화 → 동기화 문제 없음
대기열 장애 시      → DB 락이 최종 방어선
```
---

## 쟁점 2. 공정성 - 대기열 설계

### 상황

00시 동시 접속 수천 명. 대기열 없이 경쟁하면 네트워크 속도가 당락을 결정.

### 판단
- 주문서 진입 시점에 줄을 세우고, 순번이 됐을 때 결제하는 흐름

**→ GET /checkout에서 대기열 진입. 이 시점에 Booking 엔티티의 상태를 WAITING 으로 생성.**

### Checkout에서 예약을 미리 생성하는 이유

- 대기열 진입부터 예약 이력 추적 가능
- POST /bookings에서 기존 예약의 상태만 변경하는 단순한 구조
- 예약 ID를 폴링 식별자로 활용 가능

### 트레이드오프

Checkout 후 결제를 끝까지 진행하지 않으면 WAITING 예약이 누적될 수 있음.
향후 만료된 WAITING 예약 정리 배치 또는 대기열 TTL 설정으로 해소 가능.

---

## 쟁점 3. 재고 선점 타이밍

### 상황

재고 차감과 결제 중 어느 것을 먼저 처리할지 결정 필요.

### 판단

결제를 먼저 하면 여러 명이 동시에 결제를 완료한 후 재고를 초과할 가능성 존재.
재고를 먼저 선점하면 초과판매를 원천 차단할 수 있음.

**→ 재고 선점 후 결제. 결제 실패 시 재고 원복.**

### 복합결제 처리 순서

카드/Y페이(외부 결제) → 포인트(내부 차감) 순서로 처리.
외부 결제가 실패하면 포인트를 건드리지 않아 롤백 불필요.

---

## 쟁점 4. 결제 실패 처리 및 재시도 전략

### 상황

결제 실패 원인에 따라 재시도 가능 여부가 다름. 무분별한 재시도는 대기열 뒤 사람들의 대기 시간을 늘림.

### 판단

실패 원인을 재시도 가능(일시 장애, 타임아웃)과 불가(한도 초과, 카드 오류 등)로 분류.
재시도 가능한 경우 Exponential Backoff으로 최대 2회 재시도.

**→ 실패 원인 분류 후 선택적 재시도. 최대 2회.**

### 재시도 2회로 제한하는 이유

00시 트래픽 집중 상황에서 재시도가 길어지면 대기열 뒤 사람들 전체가 밀림.
빠르게 실패 판정하고 다음 사람에게 기회를 주는 것이 공정.

### PG사 응답시간 가정

```
정상 응답: 300ms
타임아웃 기준: 3초

재시도 간격:
1차 실패 → 1초 후 재시도
2차 실패 → 2초 후 재시도
3차 실패 → 포기 → 보상 트랜잭션
```

### 보상 트랜잭션

결제 최종 실패 시:

1. 예약 상태를 CANCELLED로 변경 (별도 트랜잭션으로 커밋)
2. 멱등키 삭제 → 재시도 허용 (멱등성 처리 레이어에서 자동 처리)

---

## 쟁점 5. 멱등성 처리

### 상황

결제 버튼 연속 클릭 또는 네트워크 오류로 인한 재시도 시 중복 결제 발생 가능.

### 멱등키 생성 주체

클라이언트가 생성하면 앱 종료/브라우저 닫힘으로 키를 분실했을 때 같은 주문인데 새 키로 재요청하여 중복 결제 가능.

**→ 서버가 주문서 진입(GET /checkout) 시점에 발급. 재호출 시 동일 키 반환.**

### 중복 요청 처리

- 첫 요청 → 처리 중 상태 저장 (SETNX) → 결제 진행
- 처리 중에 재요청 → 409 반환
- 완료 후 재요청 → 캐싱된 응답 반환
- 실패 시 키 삭제 → 재시도 허용

### 멱등키 구현 방식

AOP + 어노테이션으로 분리. 비즈니스 로직과 멱등성 로직 혼재를 방지.
URL 패턴 기반 Interceptor 방식은 적용 대상을 코드에서 파악하기 어렵고 누락 위험 존재.

### TTL 10분 설정 이유

00시 오픈 후 수 분 내 매진 예상. 10분이면 모든 재시도 시나리오를 커버하면서 메모리 낭비를 최소화.

### DB 저장 검토 및 기각

Redis 장애 시 fallback을 위한 DB 저장을 고려했으나 기각.
Redis 장애 시 대기열, 재고 차감도 함께 영향받아 서비스 전체가 비정상 상태이므로 멱등키 단독 fallback의 실익이 낮음.

### 서버 ↔ PG사 멱등성

PG사 결제 요청 시 예약 ID 기반 멱등키를 함께 전달.
재시도 시 동일 키 사용 → PG사가 중복 결제 방지.
현재는 Mock 구현이라 실제 동작 없음. 실제 PG사 연동 시 반드시 적용 필요.

---

## 쟁점 6. 대기열 상태 알림 - 폴링 vs SSE vs WebSocket

### 판단

SSE/WebSocket은 수천 개의 커넥션을 동시에 유지해야 하여 인프라 증설이 제한적인 상황에서 부담.
3~5초 간격 폴링은 UX상 큰 문제가 없으며 커넥션을 유지하지 않아 서버 부하가 낮음.

**→ 폴링 채택. 실시간성보다 서버 안정성 우선.**

|           | 폴링 | SSE | WebSocket |
|-----------|----|-----|-----------|
| 구현 복잡도    | 낮음 | 중간  | 높음        |
| 서버 커넥션 부담 | 낮음 | 높음  | 높음        |
| 실시간성      | 낮음 | 높음  | 높음        |

---

## 쟁점 7. Redis 장애 Fallback

### 상황

Redis 단일 인스턴스 운영으로 SPOF 존재. Redis 장애 시 대기열, 재고 선점, 멱등성 모두 영향받음.

### Redis High Availability 구성 검토 및 기각

Sentinel/Cluster 모두 추가 서버가 필요하여 "인프라 증설이 제한적인 상황" 조건에 맞지 않음.

**→ 단일 Redis 유지 + 장애 시 항목별 Fallback.**

### 항목별 Fallback 전략

| 항목     | 장애 시 대응                   |
|--------|---------------------------|
| 앱 기동   | Redis 연결 실패를 무시하고 기동 허용   |
| 대기열 진입 | 대기열/멱등키 발급 스킵. 예약은 반드시 생성 |
| 대기열 조회 | 503 반환                    |
| 멱등키 검증 | 검증 스킵                     |
| 순번 검증  | 검증 스킵                     |

### 멱등키/순번 검증 스킵의 실제 위험

잃는 것: 공정성 (순번 없이 Checkout한 사람이라면 누구든 진입 가능)

유지되는 것:

- 재고 정합성 → DB 비관적 락
- 중복 결제 방지 → PG사 멱등키
- 중복 요청 차단 → 예약 상태 체크 (CONFIRMED이면 409)

Redis 장애라는 예외 상황에서 감수 가능한 수준.

### 트레이드오프

```
가용성 우선 → 공정성 일시 포기
정합성     → DB 비관적 락으로 유지
장애 지속  → 서비스 중단 고려
```

---

## 쟁점 8. PG사 추상화 구조

### 판단

카드와 Y페이를 단일 인터페이스로 통합하면 PG사 변경 시 서로 영향받음.
각 결제 수단별 인터페이스로 분리하면 독립적 확장 가능.

**→ 카드 PG사 / Y페이 PG사 인터페이스 분리. 현재는 Mock 구현체 사용.**

실제 연동 시 각 구현체에서 PG사 에러코드를 내부 표준 에러코드로 변환.
결제 처리 로직은 에러코드 변환에 영향받지 않음.

### 포인트 처리

포인트는 외부 PG사가 아닌 내부 잔액 차감이므로 별도 처리.
실제 서비스에서 포인트 서비스가 MSA로 분리된다면 인터페이스 추가로 확장 가능.

### 재시도 위치

재시도 로직은 결제 서비스가 아닌 각 결제 수단 처리 로직에 위치.
결제 서비스는 재시도를 알 필요 없음 → 관심사 분리.

---

## 쟁점 10. 외부 API 호출과 트랜잭션 분리

### 상황

결제 요청 전체가 하나의 트랜잭션으로 묶이게 되면 PG사 호출(1~3초) 동안 DB 커넥션을 점유.
고TPS 환경에서 커넥션 풀 고갈 위험.

```
[기존] TX: 조회 → 재고차감 → PG 호출(1~3초) → DB 저장
                               ^^^^^^^^^^^^^^
                               커넥션 점유 중
```

### 판단
PG사 호출을 트랜잭션 밖으로 분리. DB 쓰기만 짧은 트랜잭션으로 처리

```
[변경] 조회 → 재고차감 → PG 호출(1~3초) → [TX: DB 저장만]
                                             ^^^^^^^^^^^^^
                                             수 ms만 점유
```

### 보상 트랜잭션

PG 호출 성공 + DB 저장 실패 시 PG사 취소 API를 호출하여 결제를 취소해야 함.
현재는 Mock 구현으로 취소 호출 로직은 구현되어 있으나 실제 PG사 연동 시 동작함.

### 트레이드오프

```
DB 커넥션 점유 시간 감소 → 고TPS 안정성 확보
트랜잭션 경계 복잡해짐  → 보상 트랜잭션 수동 처리 필요
```

---

## 쟁점 11. 대기열 이탈 처리 - TTL 만료 감지 방식

### 상황

rank 1이 된 사용자가 결제하지 않고 이탈 시 대기열이 멈추는 문제 발생 가능.
TTL 만료를 어떻게 감지하고 처리할지 결정 필요.

### 초기 접근

- GET /queue-status를 3~5초 간격으로 폴링하는 구조이므로, 폴링 요청이 들어오는 시점에 rank 1의 ready 키 만료를 감지하고 제거하는 방식을 먼저 고려
- 대기 중인 사용자가 어차피 폴링하고 있으니 자연스러운 흐름이고 구현도 단순할 것으로 판단

### 문제

```
폴링하는 사용자가 없으면 만료 감지가 안 됨
→ 뒤 사람들 전원이 이탈하면 대기열이 영구히 멈춤
→ 폴링 요청이 있을 때만 정리되는 구조는 불안정
```

### 판단

폴링 요청 시점에만 정리하는 방식은 사용자 트래픽에 의존하므로 스케줄러로 보완.

**→ 폴링 시점 정리 + 주기적 스케줄러 조합 채택.**

```
GET /queue-status 호출 시
→ rank 1의 ready 키 만료 확인
→ 만료됐으면 해당 유저 제거 + 다음 사람 승격

스케줄러 (5초 간격)
→ 대기열이 있는 상품 전체 조회
→ 각 상품의 결제 순번 사용자 대기 만료 확인
→ 만료됐으면 제거 + 다음 사람 승격
→ evict 자체가 멱등하므로 분산 락 없이 중복 실행 허용
```

### 스케줄러 상품 조회 방식

현재는 상품이 10개 한정이므로 전체 순회해도 부하 없음.
상품 수가 늘어나면 대기열이 있는 상품 ID만 별도로 관리하여 불필요한 순회를 줄이는 방향으로 개선 예정.

### Redis 장애 시

```
ready 키 확인 실패 시 → 검증 스킵
→ 공정성 일시 포기
→ DB 비관적 락이 재고 정합성 보장
```

### 트레이드오프

```
폴링 시점 정리 → 폴링 주기(최대 5초) 만큼 지연 감수
스케줄러 보완  → 사용자 트래픽 의존성 제거
```